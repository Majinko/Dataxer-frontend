export interface Contact {
  id: number;
  photoUrl?: string;
  name: string;
  street: string;
  city: string;
  country: string;
  postalCode: string;
  regNumber: string;
  email: string;
  phone: string;
}
